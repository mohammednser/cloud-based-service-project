# Netlify configuration for React + Amplify client
[build]
  command = "npm run build"
  publish = "build"

# Redirect all requests to index.html (for SPA routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
