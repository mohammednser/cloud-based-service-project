# Netlify configuration for React + Amplify client
[build]
  base = "cloud-based-service"
  command = "npm run build"
  publish = "build"

# Redirect all requests to index.html (for SPA routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
